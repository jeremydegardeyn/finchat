dbos>=2.0.0
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
google-cloud-dataplex>=2.0   # read Dataplex DQ scan results + re-run scans

# Gemini via Vertex AI (no API key) — used by the evaluator/summary when running on
# Cloud Run (GOOGLE_GENAI_USE_VERTEXAI=TRUE). The harness, demo, and tests all run
# fully offline without it.
google-genai>=0.3.0
